{
  "name": "dilivvafast-rules-test",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Security-rules tests for firestore.rules and storage.rules, run against the Firebase emulators.",
  "scripts": {
    "test": "firebase emulators:exec --project demo-dilivvafast --only firestore,storage \"node --test --test-concurrency=1 --test-reporter=spec\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
